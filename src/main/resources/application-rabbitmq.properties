# 第 27 周 RabbitMQ profile：仅在本机或部署环境已经运行 RabbitMQ 时启用。
# 启动示例：RABBITMQ_HOST=localhost mvn spring-boot:run -Dspring-boot.run.profiles=rabbitmq
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:guest}

# 由 RabbitMessagingConfig 创建的监听容器使用自动 ACK；方法正常返回才确认消息。
spring.rabbitmq.listener.simple.acknowledge-mode=auto
spring.rabbitmq.listener.simple.default-requeue-rejected=false

# RabbitMQ profile 已明确依赖 RabbitMQ，因此开启 RabbitMQ 健康检查。
management.health.rabbit.enabled=true
